<h1>Boncommande creation</h1>

<form action="{{ path('boncommande_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('boncommande') }}">
            Back to the list
        </a>
    </li>
</ul>
