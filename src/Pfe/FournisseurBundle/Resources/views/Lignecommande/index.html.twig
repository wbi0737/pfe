<h1>Lignecommande list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Quantite</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('lignecommande_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.quantite }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('lignecommande_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('lignecommande_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('lignecommande_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
